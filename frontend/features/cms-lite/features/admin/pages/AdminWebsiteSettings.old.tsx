/**
 * Legacy backup path kept to avoid broken references during cleanup.
 * The original file contained invalid byte sequences that broke Nixpacks scanning.
 */

export { default } from "./AdminWebsiteSettings/index";
